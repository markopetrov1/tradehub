import {
  SafeAreaView,
  StyleSheet,
  Text,
  TouchableOpacity,
  View,
} from "react-native";
import { colors } from "../themes/colors";

export const AddItemScreen = ({ navigation }) => {
  return (
    <View>
      <Text style={{ fontSize: 32 }}>AddItemScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
