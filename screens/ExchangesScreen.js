import {
  SafeAreaView,
  StyleSheet,
  Text,
  TouchableOpacity,
  View,
} from "react-native";
import { colors } from "../themes/colors";

export const ExchangesScreen = ({ navigation }) => {
  return (
    <View>
      <Text style={{ fontSize: 32 }}>ExchangesScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
